whybug:
  build: .
  command: bash -c ". ./envvars && bin/start.sh"
  ports:
    - "80:80"
  links:
   - db
   - search
  environment:
    MYSQL_HOST: "db"
    MYSQL_USER: "root"
    ES_URL: "search"

db:
  image: dockerfile/mariadb

search:
  image: dockerfile/elasticsearch
