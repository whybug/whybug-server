


## Installation

`npm install`

## Commands

 * `npm start` starts the server with pm2
 * `npm start` starts the server with pm2
 * `npm test` runs tests
 * `npm run watch` recompile javascript on change
